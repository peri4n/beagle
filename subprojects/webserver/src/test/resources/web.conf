ui-root = "subprojects/frontend/dist/"
port = 9000
persistence = {
  user = fbull
  database = beagle
  password = password
  execution = {
    type = fixed
    threads = 3
  }
}
search {
  index-name = "fasta"
  execution = {
    type = fixed
    threads = 1
  }
}
security {
  realm = "beagle"
  jwt {
    expiration-time = 30 minutes
    secret = "secret"
  }
}
execution = {
  type = fixed
  threads = 4
}
