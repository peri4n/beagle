ui-root = "subprojects/frontend/dist/"
port = 9000
persistence = {
  user = fbull
  database = beagle
  password = password
  exec = {
    type = fixed
    threads = 3
  }
}
search {
  index-name = "fasta"
  exec = {
    type = fixed
    threads = 1
  }
}
security {
  type = in-memory
  jwt {
    expiration-time = 30 minutes
    secret = "secret"
  }
}
exec = {
  type = fixed
  threads = 4
}
